# 作業ログ 2026-08-07 — claude-tts-extension 凍結手続き

> 承認前のため Drive には未配置。**凍結理由が未記入のため、手続きは未完了。**

## 依頼

- 案件: claude-tts-extension（Claude TTS Chrome拡張）／宛先: PM（pm）
- 内容: この案件を凍結してください。理由: ← **空欄のまま届いた**

## 実施

| # | 作業 | 結果 |
|---|---|---|
| 1 | `zashii5793/Claude-TTS-Chrome-extension` を匿名 clone | `544adcd` / 12ファイル |
| 2 | 更新状況を確認 | 最終コミット 2026-06-14。**54日間更新なし**。コミット1本（shallow） |
| 3 | 構成を確認 | `hooks/`（macOS `say` 依存）＋ `chrome-extension/`（Web Speech API） |
| 4 | 稼働コスト・依存を確認 | 課金なし・他案件からの参照なし・顧客なし |
| 5 | 凍結の実質を整理 | 停止で壊れるものが無い＝実質は記録のみ |

## 判断

**凍結理由は補わなかった。**

凍結は案件の停止であり、理由は後から「なぜ止めたのか」を説明する唯一の記録になる。推測で埋めると記録として機能しないうえ、誤った理由が定着する。そこで**理由以外をすべて用意した状態**にし、成果物 §4 に記入欄を置いた。

成果物 §3 に実測からの**理由の候補を3つ**挙げたが、候補であって決定ではない。特に「個人利用の範囲で目的を達した」のか「手が回らなかった」のかで意味が正反対になり、**再開条件の書き方が変わる**。

## 所見

1. **4案件で凍結コストが最も低い。** 課金・依存・顧客のいずれも無く、止めても動いているものは動き続ける。
2. **削除・private 化は推奨しない。** public のまま置くコストはゼロで、再開時の手間だけが増える。
3. **凍結中は壊れても誰も気づかない。** Claude Code の hooks 仕様と claude.ai の DOM 構造に依存しているため、どちらが変わっても黙って動かなくなる。凍結の理由にはならないが、**再開時は動作確認から**という前提として記録した。

## 未実施

- **リポジトリ README への凍結但し書きの追記。** 本セッションで push 権限を取得していないため未実行。承認後に指示が要る。
- **Notion 側のタスク確認・クローズ。** `company.yaml` が無く、Notion 上の呼称（`claude-tts-extension` / `Claude TTS` / `Claude-TTS-Chrome-extension`）が確定できなかったため未検索。呼称の指定があれば実施する。
- **Drive `02_プロジェクト管理/claude-tts-extension/` の作成。** 未作成だが、承認前の成果物は Drive に置かない方針のため作成していない。

## 限界

- GitHub Issue/PR は参照不可（API スコープが trust-car-platform のみ）。未クローズの Issue/PR が残っている可能性がある。
- shallow clone のため「コミット1本」は見え方であり、実履歴はより長い可能性がある。ただし最終 push が 2026-06-14 であることはリポジトリ一覧の `pushed_at` で確認済み。
- Chrome ウェブストアの公開状況は README からの推定。ストア側は未確認。

## 成果物

- `projects/claude-tts-extension/FREEZE_PROPOSAL_2026-08-07.md`（**下書き・未承認・未完成**）
