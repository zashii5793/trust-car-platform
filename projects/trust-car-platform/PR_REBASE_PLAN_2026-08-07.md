# 滞留PRを現 main で緑にするか — 判断材料と実行計画

| 項目 | 内容 |
|---|---|
| 案件 | trust-car-platform（TrustCar — B2B社用車管理への振り直し） |
| 宛先 | 開発（app-developer） |
| 状態 | **下書き（未承認）** — Drive `02_プロジェクト管理/trust-car-platform/` へは未配置 |
| 作成 | 2026-08-07 |
| 基準 | `origin/main` = `f070f2e`（2026-08-06 07:05 JST） |
| 前提資料 | `docs/OPEN_PR_TRIAGE_2026-08-05.md`（オーナー決裁済み）／ PR #114（決定A〜E） |

---

## 0. 結論サマリ（4行）

1. **「要修正4件」は #98 / #90 / #88 / #61 と特定しました**（オーナー決裁の残置8件のうち、まだ open な4件）。ただし推定のため §1 で根拠を示します。
2. **4件とも rebase が要ります。全て16〜21コミット遅れで、現在の「緑」は現 main に対する緑ではありません。**
3. **#88 は rebase する前に閉じるべきです。** 決定C の対抗案 #113 が **2026-08-05 にマージ済み**で、#88 は敗れた側の実装です。
4. **決定A〜E のうち C は消滅、E は材料が揃いました。** A・B・D は依然として社長の手番です（§4）。

---

## 1. 「要修正4件」の特定

ご指示に件数だけあり内訳が無かったため、決裁文書から逆算しました。

`docs/OPEN_PR_TRIAGE_2026-08-05.md` のオーナー決定「残す（転用・8件）」:

```
 #75  #76  #108        ← 🟢 MERGE 判定だったもの
 #35  #90  #88  #61  #98  ← 🟡 DECISION から転用と決めたもの
```

2026-08-07 実測で、このうち **open のまま残っているのは4件**です。

```
 #75   → open でない（マージ済み）
 #76   → open でない（マージ済み）
 #108  → open でない（マージ済み）
 #35   → open でない（#110 として再実装・マージ済み）
 ────────────────────────────────────────
 #98   → open   ✅ 要修正4件
 #90   → open   ✅
 #88   → open   ✅
 #61   → open   ✅
```

**ちょうど4件で一致します。** これを「要修正4件」と解釈して以下を組みました。
違う4件を指していた場合はご指摘ください（計画の骨格は変わりません）。

> なお open PR は全部で **9件**です（#118 #116 #114 #111 #106 #98 #90 #88 #61）。
> ご指示の材料欄にあった「未クローズPR 7件」とは一致しません。実測値は9件です。

---

## 2. 現 main に対する実測（これが判断の中心）

```
 PR    behind  ahead  merge      記録上のCI      CI実施日
 ────  ──────  ─────  ─────────  ─────────────  ──────────
 #98     16      4    CONFLICT   緑 8/8         2026-07-28
 #90     16      2    clean      緑 8/8         2026-07-20
 #88     16      1    clean      🔴 1件失敗      2026-07-18
 #61     21      6    CONFLICT   緑 8/8         2026-06-28
```

### ここが要点です

**4件とも「緑」ですが、その緑は最大6週間前の main に対する緑です。**

```
 #61 の緑は 2026-06-28 のもの。その後 main は 21 コミット進んでいます。
 いま現 main にのせて緑になる保証は、どこにもありません。
```

ご質問は「rebase して現 main で緑にするか」でしたが、
**現状は「緑かどうかを誰も知らない」状態**です。rebase は緑にする作業であると同時に、
**緑かどうかを知るための唯一の手段**でもあります。

### 競合の有無

```
 clean     #90  #88     → rebase は機械的に通る見込み
 CONFLICT  #98  #61     → 手で解消が要る
```

---

## 3. 実行計画（推奨順）

### Step 0 — #88 を rebase せずクローズ（推奨）

**理由: 決定C は既に決着しています。**

決定C は「Webプレビューを #88（Firebase Hosting）と #113（GitHub Pages）のどちらにするか」でした。

```
 #113  2026-08-05 07:18 マージ済み（merged_by: zashii5793）
 #88   open のまま・16コミット遅れ・CI 🔴
```

**#113 が採用され、既に main に入っています。** #88 はその対抗案でした。

さらに #88 の唯一の CI 失敗は `build_and_deploy`（Firebase Hosting へのデプロイ）で、
`FIREBASE_SERVICE_ACCOUNT` Secret 未登録が原因とみられます。
**採用しない方式のために Secret を登録するのは筋が悪い**と考えます。

> ⚠️ ただしクローズはオーナー判断です。#88 には「PRごとのプレビューチャンネル」という
> #113 に無い機能があります（#113 は main のみ）。**PR単位のプレビューが要るなら
> #88 の価値は残ります。** 要否をご判断ください。

### Step 0 の副産物 — #113 の未解決問題は解決済みでした

#113 は「`lib/` の16ファイルが `dart:io` を無条件 import しており、
`flutter build web` が通るか不明」と明記していました。

**2026-08-06 の #118 の CI で `Build Flutter Web` が success しています。**

```
 job: Build Flutter Web   conclusion: success   2026-08-06 10:30〜10:32
 run: 31093530396
```

**懸念は実測で否定されました。** Web ビルドは通ります。
条件付き import への書き換え作業は不要です。

### Step 1 — clean な #90 を先に通す

```
 #90  愛車カルテPDF出力 → B2B では「車両台帳PDF」に転用（決裁済み）
      16コミット遅れ / merge clean / 変更2コミット
```

競合が無く、変更も小さいため**最初に回して CI の基準を作る**のに向いています。
ここが緑なら「16コミット分の main の変化は、既存PRを壊していない」ことの証拠になります。
赤なら、以降の3件も同じ理由で赤になると予測でき、切り分けが一気に進みます。

```bash
git checkout claude/night-20260720
git rebase origin/main          # 競合なしの見込み
git push --force-with-lease origin claude/night-20260720
```

### Step 2 — #98（CONFLICT）

```
 #98  AppTextField 統一（Issue #29 完了を自称）
      16コミット遅れ / CONFLICT / 変更4コミット
```

決裁文書は #98 を「UI統一の集約先」と位置づけています。
競合はUI多数ファイルにまたがる見込みで、**4件の中でいちばん手がかかります。**

> ⚠️ **決定B（B2C資産の扱い）が未決のまま #98 を通すのは順序が逆です。**
> #98 は B2C 画面を含む多数のUIファイルに触れます。B-3（削除）を選ぶなら、
> **これから消す画面の整形に工数を払う**ことになります。
> 決定B の後に回すことを推奨します。

### Step 3 — #61（CONFLICT・最古）

```
 #61  ローンチ前データ運用の強化（検証・大量データ・マイグレーション設計）
      21コミット遅れ / CONFLICT / 変更6コミット / 2026-06-27 作成
```

決裁文書の評価は「法人は車両台数が多く、大量データ検証・移行設計は B2B でこそ必要」。
**4件で唯一 draft でない**＝レビュー可能な状態です。

ただし **6週間ぶんの遅れ＋競合**であり、rebase コストは4件で最大です。

### まとめ

```
 順序  PR    作業              見込み
 ────  ────  ────────────────  ──────────────────────────
  0    #88   クローズ          決定C 決着済み。要オーナー判断
  1    #90   rebase → CI       競合なし。基準づくり
  2    #98   rebase → CI       競合あり。決定B の後に回すべき
  3    #61   rebase → CI       競合あり。最古・最大
```

---

## 4. 決定A〜E の現況（PR #114 §6）

**2026-08-07 の実測で状況が変わったものがあります。**

```
 決定A  効率化の対象をどこに置くか          → 未決。社長の手番のまま
 決定B  B2C資産（SNS・マーケット）の扱い     → 未決。#98 の前提になる
 決定C  Webプレビューを #88 か #113 か       → ✅ 決着済み（#113 マージ）
 決定D  「営業／顧客ヒアリング」機能         → 未決。社長の手番のまま
 決定E  社内資料の鮮度を担保するか           → 材料が揃いました（下記）
```

### 決定E に足せる材料

PR #114 は「前回セッションの次アクション3件のうち2件が既に完了済みだった」ことを
根拠に E を提起しました。**本セッションで同種のズレがさらに3件見つかっています。**

```
 ① PR件数     ご指示「7件」 / PR#114ログ「6件」(8/05) / 実測「9件」(8/07)
 ② #106       オーナー決裁は CLOSE。しかし現在も open
 ③ #113 の懸念  「flutter build web が通るか不明」→ 8/06 に success 済み
```

**②が特に重いです。** 決裁で「クローズする」と決めた PR が、2日後も open のままです。
決裁が実行に落ちたかを誰も確認していないことになります。

> これは E-2（着手前の実測を必須化）だけでは防げません。
> **決裁の実行を確認する担当が要る**という、E に無かった論点です。

---

## 5. Notion の CI失敗タスクの統合

ご指示のとおり、**発生源（`ci.yml` / `pm_report.yml`）を直しても既存の Notion ページは
自動では消えません。** 実測結果は以下です。

### 実測

```
 trust-car-platform の CI／ワークフロー失敗タスク   18 件以上
```

ご指示の「17件」に近い値です。内訳（タイトル別）:

```
 [優先] trust-car-platform CI失敗を修正 / 修正・調査          4 件
 [優先] trust-car-platform CI失敗を恒久修正                   4 件
 [優先] trust-car-platform CI失敗ループ＆Actionsストレージ超過  4 件
 [優先] trust-car-platform Weekly PM Report ワークフロー失敗   2 件
 [優先] trust-car-platform Apple審査ブロッカー解消（CIグリーン化）1 件
 [優先] trust-car-platform CI/PMレポート復旧                   1 件
 trust-car-platform CI失敗の確認・修正（apple-review-blockers） 1 件
 trust-car-platform Web Preview(Cloudflare Pages)デプロイ失敗   1 件
 ─────────────────────────────────────────────────────  ────
                                                            18 件
```

> ⚠️ **これは正確な全数ではありません。** Notion 検索はセマンティック検索で
> 1回あたり最大25件しか返さないため、**18件は下限値**です。
> 正確な件数は Notion 側でタイトル一括検索する必要があります。

作成日は 2026-07-22 〜 2026-08-05 に集中しており、**朝のブリーフィング自動起票が
毎日同じ事象を新規ページとして起こしていた**ことが、各ページの「出典」欄から読めます
（例:「出典: GitHub通知メール」「出典: 7/20ブリーフィング優先事項」）。

### 統合案

```
 1. 代表ページを1つ残す
    → 「[優先] trust-car-platform CI/PMレポート復旧（Apple審査ブロッカー解消）」
       （3b3a67f3... / 2026-08-05 起票）が最新かつ射程が広い
 2. 残り17件以上を、代表ページへのリンクを添えてクローズ
 3. 代表ページに PR #109 の恒久修正内容と、本レポートの実測を追記
 4. 発生源を断つ（下記）
```

### 発生源について

PR #109 で `ci.yml` / `pm_report.yml` は恒久修正済みですが、
**Notion への起票そのものは別系統**です。

```
 CI の失敗          → PR #109 で修正済み（発生源は止まった）
 Notion への起票    → 朝ブリーフィングの自動化。ここは未確認
```

**GitHub 通知メールを読んで自動起票する仕組みが残っている場合、
CI が緑でも過去メールから起票が続く可能性があります。**
統合の前に、この自動化がどこで動いているかの確認が要ります
（本セッションからは特定できませんでした）。

---

## 6. ★ 本番 Firebase — 依然として誰も見られていません

PR #114 §8 が挙げた最大の限界は、**本セッションでも解消できませんでした。**

実測した接続可能性:

```
 firebase CLI                        未インストール
 gcloud CLI                          未インストール
 ~/.config/firebase                  存在しない
 ~/.config/gcloud                    存在しない
 FIREBASE_TOKEN 環境変数              未設定
 GOOGLE_APPLICATION_CREDENTIALS      未設定
```

**したがって以下は本セッションでも未取得のままです。**

```
 本番の firestore.rules      取得不可
 本番の firestore.indexes    取得不可
 本番の storage.rules        取得不可  ← データ部門が「これが取れるまで
                                          デプロイ可否は判断材料が無い」とした項目
 本番の利用実績・課金額       取得不可
```

リポジトリ側の `.firebaserc` は `{"projects":{"default":"trust-car-platform"}}` で、
プロジェクトIDは分かりますが、**認証情報が無いため接続できません。**

> **これは AI 側の工夫で回避できません。** Firebase Console にログインできる方が
> Console のルール履歴タブから取得するか、`firebase login` 済みの環境で
> `firebase firestore:indexes` / `firebase deploy --dry-run` を実行する必要があります。
> **決定A（特に A-1）とデータ部門の条件付き反対は、ここが埋まるまで解けません。**

---

## 7. この計画の限界（明示）

- **「要修正4件」は決裁文書からの逆算であり、指示に内訳が無かったため推定です。**
- **rebase 後に緑になるかは実行するまで分かりません。** 本レポートは「現在の緑は現 main
  に対する緑ではない」ことを示したもので、rebase 後の結果を予測したものではありません。
- **この環境に Flutter SDK がないため、ローカルでのテスト実行・rebase 検証をしていません。**
  behind/ahead/merge 可否は `git` の実測ですが、CI の成否は実際に回すまで不明です。
- **Notion の18件は下限値**です（検索が25件で頭打ち）。
- **本番 Firebase の状態は未取得**（§6）。判断材料の相当部分がここに依存します。
