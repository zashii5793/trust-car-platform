# 作業ログ 2026-08-05 — trust-car-platform 効率化の全部署諮問

> 案件未成立。承認前のため Drive には未配置。

## 依頼

- 社長の考え: 「全部門、trust-car-platform の案件を効率よく進める方法を模索して」
- 進め方: 関係部署を PM が選定 → 各部署の立場から論点・反対理由・足りない材料を出させる
- 制約: **結論を出さない**。社長が決められる形（選択肢と判断材料）にする。案件IDを提案する

## PM の部署選定

開発 / QA / データ / UI・UX の4部署。選定理由は成果物 §1。

**外した部署はなし。ただし、4部署のうち3部署が「足りない材料」として営業・顧客ヒアリングを挙げたのに対し、当社にその部署が定義されていない**ことを構造的な問題として報告した（成果物 決定D）。

## 実施

| 時系列 | 作業 | 結果 |
|---|---|---|
| 1 | `CLAUDE_SESSION_NOTES.md` / `docs/HUMAN_TASKS.md` から前提を抽出 | B2B振り直し中、滞留PR 38→9件、CI恒久修正、テスト3284件超 |
| 2 | 4部署へ同一フォーマットで諮問（論点／反対理由／足りない材料） | 4部署とも回答 |
| 3 | **UI・UX から「前提が古い」との指摘** | 裏取りへ |
| 4 | `git fetch origin main` して実測検証 | **指摘はすべて事実**（下記） |
| 5 | GitHub の Open PR / Issue を実測 | PR 6件・Issue 12件 |
| 6 | 諮問結果を統合 | `projects/trust-car-platform/DEPARTMENT_CONSULTATION_2026-08-05.md` |

## 前提の訂正（最重要）

社内資料 `CLAUDE_SESSION_NOTES.md` の記載が古かった。実測での訂正:

| 資料の記載 | 実測（`origin/main`） |
|---|---|
| 次アクション「#109 → #76 → #75 → #108 をマージ」 | 4本とも**マージ済み**（`676289a` / `595c82a` / `b899363` / `d680ec3`） |
| 次アクション「#35 を B2B前提で切り出し再実装」 | **#110 として実施済み・マージ済み**（`7a47f58`） |
| 滞留PR 9件 | **6件**（#113 #111 #98 #90 #88 #61） |
| 店舗比較画面は未実装 | **実装済み**（`lib/screens/shop/shop_comparison_screen.dart` 414行、`shop_list_screen.dart:177` から配線済み） |
| ブレークポイント定義済み | 定義は `DESIGN_SYSTEM.md:527` のみ。`lib/` での使用 **0件** |

検証コマンド: `git fetch origin main` → `git log --oneline origin/main`、`git cat-file -t <sha>`、`grep -rn "Breakpoints" lib/`（0件）、`mcp__github__list_pull_requests` / `list_issues`。

## 4部署が独立に一致した点

1. **詰まりは開発側ではなく人間側**（週次PMレポート #112: HUMAN_TASKS **29件残り・完了0件**、設定ファイル2種とも未配置）
2. **「反応がなければ凍結」の判定基準が言語化されていない**（データ部門: B2B系イベントが `analytics_service.dart` に一切なく、**数字で言えない**）
3. **効率化＝削除・整理は不可逆**（QA: 30件クローズのうち #70/#96/#79 は**テストが主成果物**だった）

## 部署間の対立（社長判断が要る）

- **A**: 未デプロイ分を一括で出すか（開発・QA）／経路を絞るか（データが反対。`storage.rules` の本番乖離疑い）
- **B**: 効率化の効き先が実装側にあるか（開発「無い」／QA「むしろ検証が薄くなった」／UI・UX「より上流のIA不整合が温存される」）
- **C**: B2C資産の扱い（残置／FeatureFlag拡張／削除）

## 成果物

- `projects/trust-car-platform/DEPARTMENT_CONSULTATION_2026-08-05.md`（**下書き・未承認**）
  - §6 に決定A〜E（各選択肢＋判断材料＋支持/反対部署）
  - §7 に案件ID案3件

## 案件ID 提案

| ID | 射程 |
|---|---|
| **`20260805-trustcar-unblock`**（PM推し） | HUMAN_TASKS 29件の解消に絞る |
| `20260805-trustcar-b2b-pivot` | B2B転換の是非・IA転置・凍結判断 |
| `20260805-trustcar-devflow` | CI・ブランチ・資料鮮度 |

3つを1案件にまとめないことを推奨（投資先が正反対で、まとめると部署の反対意見が社長に届かなくなる）。

## この諮問の限界

- **本番Firebase の状態は誰も見ていない。** ルール・インデックス・Storage・利用実績・課金額は全て Console 権限者しか取れず未取得。判断材料の相当部分がここに依存する。
- 各部署の回答はポジショントークとして出させたもので、中立評価ではない。
- 結論・優先順位は意図的に書いていない（社長の指示による）。
