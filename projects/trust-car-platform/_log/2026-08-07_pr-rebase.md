# 作業ログ 2026-08-07 — trust-car-platform 滞留PRの rebase 判断

> 承認前のため Drive `02_プロジェクト管理/trust-car-platform/_log/` には未配置。

## 依頼

- 社長の手番: 決定A〜E（PR #114）と、要修正4件を rebase して現mainで緑にするか
- Notion のCI失敗17件は発生源を断っても自動では消えないため手で統合が要る

## 実施

| # | 作業 | 結果 |
|---|---|---|
| 1 | open PR を実測 | **9件**（#118 #116 #114 #111 #106 #98 #90 #88 #61） |
| 2 | 全9件の CI check runs を取得 | 緑7 / 赤1（#88）/ 実行中1（#116） |
| 3 | `git fetch` 後 behind/ahead/merge を実測 | CONFLICT 3件（#106 #98 #61） |
| 4 | 「要修正4件」を決裁文書から逆算 | #98 #90 #88 #61 と特定 |
| 5 | 決定C の現況を確認 | **#113 が 2026-08-05 にマージ済み → 決定C 決着** |
| 6 | #113 の未解決懸念を確認 | **#118 の `Build Flutter Web` が success（8/06）→ 懸念解消** |
| 7 | Notion の CI失敗タスクを検索 | **18件以上**（検索が25件で頭打ちのため下限値） |
| 8 | 本番 Firebase への接続可否を確認 | **接続不可**（CLI・認証情報とも無し） |

## 主な発見

1. **現在の「緑」は現 main に対する緑ではない。** 要修正4件は16〜21コミット遅れで、#61 の緑は 2026-06-28 のもの。rebase は緑にする作業であると同時に、緑かどうかを知る唯一の手段。
2. **#88 は rebase 前に閉じるべき。** 決定C の対抗案 #113 が既にマージされており、#88 は敗れた側。唯一の CI 赤（`build_and_deploy`）も未採用方式の Secret 未登録が原因。
3. **#113 が「不明」としていた `flutter build web` は通る。** 8/06 の #118 CI で success 済み。`dart:io` 16ファイルの条件付き import 対応は不要。
4. **#106 は決裁で CLOSE と決まったのに open のまま。** 決裁が実行に落ちたかを誰も確認していない。
5. **本番 Firebase は本セッションでも取得不可。** PR #114 §8 の限界は解消せず。

## 未解決 / 要確認

| # | 事項 | 影響 |
|---|---|---|
| A | 「要修正4件」の内訳（本ログは決裁文書からの逆算） | 計画の骨格は変わらないが確認したい |
| B | #88 を閉じるか（PR単位プレビューが要るなら残す価値あり） | オーナー判断 |
| C | 決定B（B2C資産の扱い）— #98 の前提 | 未決のまま #98 を rebase すると、消す画面の整形に工数が乗る |
| D | Notion への自動起票がどこで動いているか | CI を直しても起票が続く可能性 |
| E | 本番 Firebase の rules / indexes / storage | 決定A とデータ部門の条件付き反対が解けない |

## 成果物

- `projects/trust-car-platform/PR_REBASE_PLAN_2026-08-07.md`（**下書き・未承認**）

## 限界

- Flutter SDK が無く、rebase 後の CI 結果は予測していない（現状の可視化のみ）。
- Notion 18件は下限値。正確な全数は Notion 側でのタイトル一括検索が要る。
- 本番 Firebase 未取得。
