# 未マージPR トリアージ — 2026-08-22

**対象**: 28本（`gh pr list --state open` の実測）
**前回**: [`OPEN_PR_TRIAGE_2026-08-05.md`](OPEN_PR_TRIAGE_2026-08-05.md)
**目的**: 「どれを閉じ、どれを通し、どれを待つか」を1枚で決める

---

## 全体

```
 28本 ＝ dependabot 15本 ＋ 人間 13本
```

7月レポートで「最大のボトルネック」と書いた滞留は、**18 → 28 に増えている。**
**作ったものが main に入らないので、実機で確かめられない。**
実機で確かめられないから、今回のような「効いていない機能」が見つからない。

---

## 1. dependabot（15本）— CIの結果で2つに分かれる

全部 **MERGEABLE**。CI の失敗有無で扱いを変える。

### 1.1 CI が全部通っている（7本）— **まとめて通してよい**

| # | 中身 |
|---|---|
| 127 | `actions/setup-node` 4 → 7 |
| 128 | `actions/github-script` 7 → 9 |
| 129 | `actions/checkout` 4 → 7 |
| 130 | `actions/deploy-pages` 4 → 5 |
| 131 | `codecov/codecov-action` 4 → 7 |
| 134 | `cupertino_icons` 1.0.8 → 1.0.x |
| 137 | `intl` 0.20.2 → 0.20.3 |
| 138 | `google_mlkit_text_recognition` |

**GitHub Actions のバージョン上げが5本**。アプリの挙動に影響しない。
`cupertino_icons` と `intl` はパッチ相当。

> **ただし iOS ラベルが無いので、iOS ビルドは検証されていない。**
> `google_mlkit_text_recognition` は OCR（iOS/Android 専用機能）なので、
> **138 だけは `ios` ラベルを付けてから通すこと。**

### 1.2 CI が落ちている（7本）— **中身を見るまで通さない**

| # | 中身 | 注意 |
|---|---|---|
| 132 | `image_picker` 1.2.1 → 1.2.3 | |
| 133 | `printing` 5.14.2 → 5.15.0 | PDF出力に効く |
| 135 | `camera` 0.11.3 → **0.12.0** | **メジャー相当。** 車検証OCRの撮影に効く |
| 136 | `image` 4.5.4 → **4.9.1** | 画像処理 |
| 139 | `path_provider` 2.1.5 → 2.1.6 | |
| 140 | `mockito` 5.6.3 → 5.8.1 | テスト側 |
| 141 | `firebase_core` 4.9.0 → **4.13.0** | **下記参照** |

**`camera` は 0.11 → 0.12 でメジャー相当の変更。** 車検証OCRは
`HUMAN_TASKS.md` P1-9 で「実画像精度は完全に未検証」とされている機能なので、
**上げる前に実機で1回撮ること。**

**`firebase_core` 4.9.0 → 4.13.0 は、いま急がない。**
iOS が起動しない原因（`GoogleService-Info.plist` が別アプリのもの・P1-8）は
**バージョンではなく設定の食い違い**なので、上げても直らない。
**設定を直してから上げる**ほうが、切り分けが濁らない。

---

## 2. 人間のPR（13本）

### 2.1 いま通すもの

| # | 日付 | 状態 | 中身 |
|---|---|---|---|
| **149** | 08-21 | ready・`ios` | 空表示の行き止まり7画面／チップの潰れ／車種フィルター／UX検査4本 |

main を取り込み済み。**4,081件パス・analyze クリーン。**

### 2.2 古いドラフト（7本）— **閉じるか、着手するかを決める**

| # | 日付 | 経過 | 中身 |
|---|---|---|---|
| 61 | 06-27 | **約2か月** | ローンチ前データ運用の強化 |
| 88 | 07-18 | 約1か月 | プレビュー自動デプロイ ＋ Issue #29 |
| 90 | 07-20 | 約1か月 | Issue #64 愛車カルテ PDF出力 |
| 98 | 07-28 | 約1か月 | AppTextField の fillColor 等 |
| 106 | 08-02 | 3週間 | Issue #41 近隣工場 Google Maps |
| 119 | 08-07 | 2週間 | ZAXEL 4案件の下書き（未承認） |
| 126 | 08-09 | 2週間 | 工場詳細画面に地図プレビュー（Issue #43） |

**106 と 126 は同じ地図まわり。** `HUMAN_TASKS.md` P0-3（Maps APIキー未設定）が
片付いていないので、**通しても地図は出ない。** キーを入れてから判断するほうが速い。

**90（PDF出力）は、いま出せる。** PDF出力の実装は完了しており、
今回 #149 で「記録0件のときボタンが理由を言う」ようにした箇所と隣接する。

### 2.3 最近のもの（5本）

| # | 日付 | 状態 | 中身 |
|---|---|---|---|
| 114 | 08-05 | ready | faq-platform 残タスク棚卸し ＋ trust-car |
| 142 | 08-09 | draft | オンボーディング文言を当初コンセプトに修正 |
| 144 | 08-12 | draft・**衝突** | 今週末の人間タスク実施プラン |
| 145 | 08-15 | draft・**衝突** | 日本語OCR修正＋カタログ外入力＋機能配置換え |
| 148 | 08-20 | draft | 事業フォーカス戦略 |

**145 は中身が大きい**（OCR・入力・配置換え）。衝突しているので、
**#149 が入ったあとに取り込み直す**のが順当。

---

## 3. 順番

```
 1  #149 を通す                    実機で確かめる前提が整う
 2  dependabot の 7本（CI通過）      機械的。ただし 138 は ios ラベルを付ける
 3  #90（PDF出力）                  いま出せる。#149 と隣接
 4  P1-8（plist）を直す             iOS が起動しない。ここが片付くまで実機検証は不能
 5  P0-3（Maps キー）を入れる        入れてから 106・126 を判断
 6  #145 を取り込み直す             #149 のあと
 7  古いドラフト 61/88/98/119 の要否 2か月放置。閉じるか着手か
 8  dependabot の 7本（CI落ち）      camera と firebase_core は実機確認とセット
```

---

## 4. 決めていただきたいこと

```
 □ 61 / 88 / 98 / 119 を閉じるか、着手するか（2か月〜1か月放置）
 □ camera 0.11 → 0.12 を上げるか（車検証OCRの撮影に効く。実機確認が要る）
 □ firebase_core の更新は P1-8 のあとでよいか
```

---

*本トリアージは 2026-08-22 時点の `gh pr list` 実測に基づく。*
*CI の可否は `statusCheckRollup` の FAILURE 件数で判定した。*
