# Morning Briefing — 2026-07-29

Generated by night agent on `claude/night-20260729`.

---

## Summary

2件の修正を完了しました。どちらも既存Issueで認識済みの問題です。

---

## Task 1: pm_report.yml CI 7週連続失敗を修正 ✅

**背景**: 週次PMレポートCI（`pm_report.yml`）が2026-06-15以降7週連続で失敗していた。PRs #89/#92/#94/#95/#97 で修正が試みられたが、いずれも他変更と混在しておりマージされていなかった。

**根本原因（4件）**:
1. `$(grep -c ... || echo "0")` がサブシェル内で `0\n0` を二重出力 → `GITHUB_OUTPUT`破損
2. `grep -oP '\-\K[0-9]+:'` の `-` が grep のオプションフラグとして解釈される
3. HUMAN_TASKS.md の行数カウントで同様の二重出力バグ
4. `pm-report`/`weekly` ラベルが未存在の場合に 422 エラーで作成ステップがクラッシュ

**修正内容**: `.github/workflows/pm_report.yml` のシェルスクリプト修正（単一ファイルのみ、最小差分）

**コミット**: `d39c0d3`

---

## Task 2: B2Cプレミアム購入経路の不在（dead-end）を修正 ✅

**背景**: App Store 審査準備レポート（#99）にて「B2Cプレミアム購入経路不在」が指摘されていた。
`_showUpgradeDialog` が「閉じる」ボタンのみのダイアログを表示しており、ユーザーがプレミアムプランを購入できない状態だった。

**修正内容**:
- `RevenueCatService.purchaseUserPremium()` — B2C専用の購入メソッド（TDD実装）
- `RevenueCatService.btocPremiumProductId` — `trustcar_btoc_premium_monthly`
- `RevenueCatService.btocPremiumEntitlementId` — `btoc_premium`
- `UserSubscriptionProvider.purchasePremium()` — RevenueCatServiceへの委譲メソッド
- `UserSubscriptionProvider` — RevenueCatService をコンストラクタ注入（後方互換）
- `home_screen.dart _showUpgradeDialog` — 「プレミアムに登録する」ボタン追加（成功/失敗 SnackBar付き）
- `main.dart` — UserSubscriptionProvider に RevenueCatService を DI

**テスト**: 6件追加 → 全3511件パス、`flutter analyze` クリーン

**コミット**: `3a90674`

---

## 残存する人間タスク（ブロッカー）

| タスク | 状態 |
|--------|------|
| `google-services.json`（Android） | ⚠️ 仮ファイル（本番版に要更新）|
| `GoogleService-Info.plist`（iOS） | 確認要 |
| RevenueCat製品ID `trustcar_btoc_premium_monthly` をApp Store Connect / Google Play Console に登録 | ❌ 未実施（本実装の前提条件） |
| RevenueCat entitlement `btoc_premium` をダッシュボードに登録 | ❌ 未実施 |
| 20+ 件の未マージPR | 人間によるレビュー・マージが必要 |

---

## Branch / PR

Branch: `claude/night-20260729`

Push後にDraft PRを作成予定。
